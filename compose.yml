services:
  server:
    build:
      context: .
    environment:
      NODE_ENV: production
      DB_HOST: localhost
      DB_USERNAME: postgres
      DB_PASSWORD: 
      DB_DIALECT: postgres
      DB_PORT: 5432
      TEST_DB_NAME: kigaluxe_test
      DEVELOP_DB_NAME: kigaluxe
      PRODUCTION_DB_NAME: kigaluxe_pro
      SECRET_KEY: secret123
      GMAIL_USER: adolphengoga@gmail.com
      GMAIL_PASS: mkks hkna twhc qzka
    ports:
      - '3000:3000'
    depends_on:
      - postgres
    networks:
      - app-network

  postgres:
    image: postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 
      POSTGRES_DB: kigaluxe
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - app-network

volumes:
  db-data:

networks:
  app-network: